spring:
  data:
    mongodb:
      uri: mongodb://localhost:27017
      database: MicroServices
  application:
    name: RATING-SERVICE
     
server:
  port: 8083 
  
  application:
    name: RATING-SERVICE
  
  
eureka:
  instance:
    prefer-ip-address: true
  client:
    fetch-registry: true
    register-with-eureka: true
    service-url:
      defaultZone: http://localhost:8761/eureka
      
okta:
  oauth2:
    issuer: https://dev-25262808.okta.com/oauth2/default
    audience: api://default        
       